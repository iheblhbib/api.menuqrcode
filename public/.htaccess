RewriteEngine On

# Route everything through the front controller except real files (e.g. uploaded images).
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
